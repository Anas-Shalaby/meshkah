# 🤝 دليل المساهمة في Meshkah

شكراً لاهتمامك بالمساهمة في **Meshkah**! نحن سعداء جداً لوجودك هنا.

## 📋 جدول المحتويات

- [قواعد السلوك](#قواعد-السلوك)
- [كيف أبدأ؟](#كيف-أبدأ)
- [عملية المساهمة](#عملية-المساهمة)
- [معايير الكود](#معايير-الكود)
- [إرشادات الـ Commit](#إرشادات-الـ-commit)
- [إرشادات الـ Pull Request](#إرشادات-الـ-pull-request)

---

## 📜 قواعد السلوك

هذا المشروع يلتزم بمدونة قواعد السلوك. عند المشاركة، يُتوقع منك الحفاظ على هذه المدونة.

### القيم الأساسية

- ✅ **الاحترام**: كن محترماً في جميع التفاعلات
- ✅ **التعاون**: اعمل معاً لتحسين المشروع
- ✅ **الانفتاح**: كن منفتحاً على الأفكار والاقتراحات
- ✅ **الصبر**: كن صبوراً مع المبتدئين

---

## 🚀 كيف أبدأ؟

### 1. إعداد البيئة المحلية

```bash
# استنساخ المستودع
git clone https://github.com/Anas-Shalaby/meshkah.git
cd meshkah

# إعداد Backend
cd backend
npm install
cp .env.example .env
# تعديل ملف .env

# إعداد Frontend
cd ../frontend
npm install
cp .env.example .env
# تعديل ملف .env

# إعداد Dashboard
cd ../dashboard
npm install
cp .env.example .env
# تعديل ملف .env
```

### 2. العثور على مهمة للمساهمة

- تحقق من [Issues](https://github.com/Anas-Shalaby/meshkah/issues) المفتوحة
- ابحث عن issues مع label `good first issue` للمبتدئين
- أو أنشئ issue جديد إذا وجدت مشكلة أو ميزة جديدة

### 3. إنشاء Branch جديد

```bash
git checkout -b feature/your-feature-name
# أو
git checkout -b fix/your-bug-fix
```

---

## 🔄 عملية المساهمة

### الخطوات الأساسية

1. **Fork** المستودع
2. **Clone** الـ fork الخاص بك
3. **إنشاء branch** جديد للميزة/الإصلاح
4. **إجراء التغييرات** مع اتباع معايير الكود
5. **اختبار التغييرات** محلياً
6. **Commit** التغييرات مع رسالة واضحة
7. **Push** إلى الـ fork الخاص بك
8. **فتح Pull Request**

### مثال على Workflow

```bash
# 1. إنشاء branch جديد
git checkout -b feature/add-new-feature

# 2. إجراء التغييرات
# ... تعديل الملفات ...

# 3. إضافة التغييرات
git add .

# 4. Commit مع رسالة واضحة
git commit -m "feat: إضافة ميزة جديدة للمخيمات"

# 5. Push إلى fork
git push origin feature/add-new-feature

# 6. فتح Pull Request على GitHub
```

---

## 💻 معايير الكود

### JavaScript/TypeScript

- استخدم **ES6+** syntax
- استخدم **async/await** بدلاً من callbacks
- استخدم **const/let** بدلاً من var
- اتبع **ESLint** rules
- استخدم **Prettier** للـ formatting

### React Components

```jsx
// ✅ جيد
const MyComponent = ({ title, onAction }) => {
  const [state, setState] = useState(null);

  useEffect(() => {
    // Logic
  }, []);

  return (
    <div className="container">
      <h1>{title}</h1>
      <button onClick={onAction}>Action</button>
    </div>
  );
};

// ❌ سيء
function MyComponent(props) {
  var state = null;
  // ...
}
```

### Naming Conventions

- **Components**: PascalCase (`MyComponent.jsx`)
- **Functions**: camelCase (`getUserData`)
- **Constants**: UPPER_SNAKE_CASE (`API_BASE_URL`)
- **Files**: kebab-case أو camelCase (`user-service.js`)

### التعليقات

```javascript
// ✅ جيد - شرح لماذا، وليس ماذا
// نستخدم Redis cache لتقليل الضغط على قاعدة البيانات
const cachedData = await redis.get(key);

// ❌ سيء - وصف واضح من الكود نفسه
// جلب البيانات من Redis
const cachedData = await redis.get(key);
```

### Arabic Code Comments

- استخدم **العربية** للتعليقات في الكود
- استخدم **الإنجليزية** للأسماء المتغيرة والدوال
- استخدم **العربية** لرسائل الأخطاء الموجهة للمستخدم

---

## 📝 إرشادات الـ Commit

### تنسيق رسالة الـ Commit

نستخدم [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Types

- `feat`: ميزة جديدة
- `fix`: إصلاح bug
- `docs`: تغييرات في التوثيق
- `style`: تغييرات في التنسيق (لا تؤثر على الكود)
- `refactor`: إعادة هيكلة الكود
- `test`: إضافة أو تعديل الاختبارات
- `chore`: مهام صيانة

### أمثلة

```bash
# ميزة جديدة
git commit -m "feat(camps): إضافة نظام الاختبارات اليومية"

# إصلاح bug
git commit -m "fix(auth): إصلاح مشكلة تسجيل الدخول"

# تحسينات
git commit -m "refactor(api): تحسين أداء API endpoints"

# توثيق
git commit -m "docs(readme): تحديث دليل البدء السريع"
```

### رسائل Commit جيدة

```bash
✅ "feat(camps): إضافة نظام الاختبارات اليومية"
✅ "fix(auth): إصلاح تسرب الذاكرة في JWT middleware"
✅ "docs(api): تحديث توثيق Quran Camps API"
```

### رسائل Commit سيئة

```bash
❌ "update"
❌ "fix bug"
❌ "changes"
❌ "WIP"
```

---

## 🔍 إرشادات الـ Pull Request

### قبل فتح PR

- ✅ تأكد من أن الكود يعمل محلياً
- ✅ أضف/حدث الاختبارات إذا لزم الأمر
- ✅ تأكد من أن جميع الاختبارات تمر
- ✅ تحديث التوثيق إذا لزم الأمر
- ✅ اتبع معايير الكود

### عنوان PR

استخدم نفس تنسيق رسائل الـ Commit:

```
feat(camps): إضافة نظام الاختبارات اليومية
fix(auth): إصلاح مشكلة تسجيل الدخول
```

### وصف PR

```markdown
## 📋 الوصف

وصف واضح للتغييرات

## 🔗 Related Issues

Closes #123

## ✅ Checklist

- [ ] الكود يعمل محلياً
- [ ] الاختبارات تمر
- [ ] التوثيق محدث
- [ ] لا توجد أخطاء في الـ linter
```

### Review Process

1. **Automated Checks**: يجب أن تمر جميع الـ checks
2. **Code Review**: سيتم مراجعة الكود من قبل Maintainers
3. **Feedback**: قد يتم طلب تغييرات
4. **Approval**: بعد الموافقة، سيتم دمج PR

---

## 🧪 الاختبار

### Backend Tests

```bash
cd backend
npm test
```

### Frontend Tests

```bash
cd frontend
npm test
```

### كتابة Tests جديدة

```javascript
// مثال على Test
describe("Camp Service", () => {
  it("should create a new camp", async () => {
    const camp = await createCamp(mockData);
    expect(camp).toBeDefined();
    expect(camp.name).toBe(mockData.name);
  });
});
```

---

## 📚 الموارد الإضافية

- [GitHub Flow](https://guides.github.com/introduction/flow/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [React Best Practices](https://react.dev/learn)
- [Node.js Best Practices](https://github.com/goldbergyoni/nodebestpractices)

---

## ❓ أسئلة شائعة

### كيف أختار issue للمساهمة؟

- ابحث عن `good first issue` label
- ابدأ بمشاكل صغيرة
- اسأل في الـ issue إذا كان لديك أسئلة

### ماذا لو كان لدي سؤال؟

- افتح [Discussion](https://github.com/Anas-Shalaby/meshkah/discussions)
- أو اسأل في الـ issue المحدد

### كيف أضيف ميزة جديدة؟

1. افتح issue أولاً لمناقشة الميزة
2. انتظر الموافقة
3. ابدأ بالتنفيذ
4. افتح PR

---

## 🙏 شكراً!

شكراً لمساهمتك في جعل **Meshkah** أفضل! 🎉

---

**ملاحظة**: هذا الدليل قابل للتحديث. إذا كان لديك اقتراحات لتحسينه، يرجى فتح issue أو PR.
